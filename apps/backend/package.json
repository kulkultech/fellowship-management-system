{
  "name": "@kulkul/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/api",
    "build": "go build -o bin/api ./cmd/api",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "seed": "go run ./cmd/seed"
  }
}
